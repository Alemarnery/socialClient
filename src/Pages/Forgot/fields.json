{
  "fields": [
    {
      "title": "email",
      "name": "email",
      "placeholder": "email",
      "validation": {
        "required": "Email is required",
        "pattern": {
          "value": "^[^@ ]+@[^@ ]+.[^@ .]{2,}$",
          "message": "Must be a valid email"
        }
      }
    }
  ]
}
